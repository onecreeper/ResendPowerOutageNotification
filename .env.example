# 电力监控系统 - 环境变量配置示例
# 复制此文件为 .env 并填入你的实际配置

# === 必需配置 ===

# Resend API Key
# 从 https://resend.com/dashboard/api-keys 获取
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxxxx

# 发件人地址
# 格式: 显示名称 <email@verified-domain.com>
# 注意: @后面的域名必须在 Resend 上验证过
SENDER_FROM_ADDRESS=服务器警卫 <alerts@your-verified-domain.com>

# 收件人邮箱
# 接收通知的邮箱地址
RECIPIENT_EMAIL=your_personal_email@example.com

# === 可选配置 ===

# 服务器名称
# 用于在邮件中标识不同的服务器
SERVER_NAME=测试服务器

# 断电判定阈值（秒）
# 默认: 180 (3分钟)
# 超过此时间没有心跳则判定为异常断电
OUTAGE_THRESHOLD=180

# 网络异常判定阈值（秒）
# 默认: 300 (5分钟)
# 网络状态数据超过此时间未更新则认为可能异常
NETWORK_OUTAGE_THRESHOLD=300

# === 网络检测配置 ===

# 内网检测目标
# 逗号分隔的多个地址，任一可达即认为内网正常
INTERNAL_TARGETS=192.168.1.1,192.168.0.1

# 外网检测目标
# 逗号分隔的多个地址，任一可达即认为外网正常
EXTERNAL_TARGETS=114.114.114.114,223.5.5.5,baidu.com

# DNS 检测目标
# 用于测试 DNS 解析功能
DNS_TARGET=baidu.com

# === 时区配置 ===

# 时区设置
# 确保邮件中的时间与你本地一致
TZ=Asia/Shanghai

# === Docker 部署配置 ===

# 在 docker-compose.yml 中使用环境变量时：
# - 复制 .env 文件到项目根目录
# - 在 docker-compose.yml 中使用 ${RESEND_API_KEY} 等变量
# - 或者直接在 docker-compose.yml 中配置（见示例）

# === Windows PowerShell 设置示例 ===

# $env:RESEND_API_KEY="re_xxxxxxxxxxxxxxxxxxxxxx"
# $env:SENDER_FROM_ADDRESS="服务器警卫 <alerts@your-verified-domain.com>"
# $env:RECIPIENT_EMAIL="your@email.com"

# === Linux/Mac Bash 设置示例 ===

# export RESEND_API_KEY="re_xxxxxxxxxxxxxxxxxxxxxx"
# export SENDER_FROM_ADDRESS="服务器警卫 <alerts@your-verified-domain.com>"
# export RECIPIENT_EMAIL="your@email.com"